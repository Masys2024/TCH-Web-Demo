"use client";

import * as React from "react";
import {
  Sidebar,
  SidebarContent,
  SidebarFooter,
  SidebarHeader,
  SidebarRail,
} from "@/components/ui/sidebar";
import { NavMain } from "./nav-main";
import { SIDEBAR_NAVLINKS } from "@/constants/navLinks";
import { SidebarLogo } from "@/components/blocks/logo";
import { NavUser } from "./nav-user";

export function AppSidebar({ ...props }) {
  const navigationData = SIDEBAR_NAVLINKS;
  return (
    <Sidebar collapsible="icon" {...props}>
      <SidebarHeader>
        <SidebarLogo />
      </SidebarHeader>
      <SidebarContent>
        <NavMain items={navigationData} />
      </SidebarContent>
      <SidebarFooter>
        <NavUser />
      </SidebarFooter>
      <SidebarRail />
    </Sidebar>
  );
}
